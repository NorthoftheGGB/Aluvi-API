<%= form_for(@payout) do |f| %>
  <% if @payout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payout.errors.count, "error") %> prohibited this payout from being saved:</h2>

      <ul>
      <% @payout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :driver_id %><br />
    <%= f.number_field :driver_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :amount_cents %><br />
    <%= f.number_field :amount_cents %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
