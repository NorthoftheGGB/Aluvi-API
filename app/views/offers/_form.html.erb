<%= form_for(@offer) do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offered_ride from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :driver_id %><br />
    <%= f.number_field :driver_id %>
  </div>
  <div class="field">
    <%= f.label :fare_id %><br />
    <%= f.number_field :fare_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
