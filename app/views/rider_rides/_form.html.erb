<%= form_for(@rider_ride) do |f| %>
  <% if @rider_ride.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rider_ride.errors.count, "error") %> prohibited this rider_ride from being saved:</h2>

      <ul>
      <% @rider_ride.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :ride %><br />
    <%= f.text_field :ride %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
